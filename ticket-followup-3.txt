Здравствуйте.

Привязал 186.246.14.127 и 186.246.14.113 к серверу NeuroTest 1 через UI — оба показывают "Подключен к сервису = NeuroTest 1". Но фактически routing к ним не работает: после reboot эти IP недоступны даже изнутри самого VPS.

Контрольные тесты с самого NeuroTest 1 (2026-05-12 13:08 UTC):

  ping 5.129.207.254  → OK (primary этой же VM)
  ping 92.53.115.87   → OK (другая моя VM в SPB-3, RTT 51 ms)
  ping 186.246.14.127 → 100% packet loss
  ping 186.246.14.113 → 100% packet loss

То есть проблема локализована строго на двух новых secondary-биндингах — не на VM, не на NSK-1, не на аккаунте.

Дополнительные диагностические факты:

  - cloud-init metadata: ds.meta_data.network отсутствует
    (VM не получила инфу о новых IP при boot)
  - netplan /etc/netplan/50-cloud-init.yaml: только primary IP
  - ip addr show eth0: только 5.129.207.254 (нет secondary aliases)
  - ip route show: нет маршрутов для 186.246.14.0/24

То есть UI пометил привязку успешной, но фактическое L3-routing и metadata-propagation для этих двух адресов не выполнились на бэкенде.


Прошу:

1. Проверить реальное состояние биндингов 186.246.14.127 и 186.246.14.113 в вашей внутренней системе (не UI, а networking layer). Что показывает provisioning state?

2. Принудительно re-provision routing для них на бэкенде — без необходимости отвязывать/привязывать через UI с моей стороны (это приведёт к ещё одному reboot без гарантии исхода).

3. Если проблема системная — посмотрите failed jobs в очереди provisioning для моего аккаунта за период 2026-05-12 11:00–13:10 UTC.

После исправления отпишите — проверю self-ping и продолжу план миграции.

Готов предоставить дополнительные данные (полный ip addr / ip route / journalctl -u systemd-networkd / cloud-init logs) по запросу.
